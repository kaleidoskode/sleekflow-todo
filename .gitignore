# Reference material, not part of the deliverable: the assignment brief (their
# document), its extracted text, and working screenshots. Kept on disk, out of
# the repository.
ref/

# Long-form reasoning behind the decision log. Kept as personal notes for the
# live discussion rather than shipped — the log itself states every decision.
docs/engineering-notes.md

# Day-one spec and task plan. Superseded by docs/decision-log.md, and stale
# against the shipped app.
docs/superpowers/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Node
node_modules/
dist/
.vite/

# Environment
# The root .env only overrides the Compose host port (DB_PORT) for machines
# where 5432 is already taken; .env.local holds one machine's credentials.
/.env
.env.local

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
.superpowers/
